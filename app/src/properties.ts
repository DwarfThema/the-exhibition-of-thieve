export interface IPos {
  x: number;
  y: number;
  z: number;
}
export interface IRot {
  x: number;
  y: number;
  z: number;
}
export interface IScale {
  x: number;
  y: number;
  z: number;
}
export const theme = {
  mainColor: "gray",
};
